{
  "name": "net-artisan-collective-threejs-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5000",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "start": "vite",
    "postinstall": "npm run build",
    "serve-heroku": "vite --host 0.0.0.0 --port $PORT preview"
  },
  "devDependencies": {
    "@types/three": "^0.149.0",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "three": "^0.150.1",
    "vite": "^4.2.0"
  }
}
